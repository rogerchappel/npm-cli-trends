name: Refresh snapshot

on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

concurrency:
  group: refresh-snapshot
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20

      - name: Prepare persistent review branch
        env:
          REVIEW_BRANCH: automation/refresh-snapshot
        run: |
          if git ls-remote --exit-code --heads origin "$REVIEW_BRANCH" >/dev/null; then
            git fetch origin "$REVIEW_BRANCH"
            git switch --create "$REVIEW_BRANCH" --track "origin/$REVIEW_BRANCH"
            git merge --no-edit "origin/${{ github.event.repository.default_branch }}"
          else
            git switch --create "$REVIEW_BRANCH"
          fi

      - name: Refresh public npm metadata
        run: npm run fetch

      - name: Verify generated artifacts
        run: npm run release:check

      - name: Publish review branch
        env:
          REVIEW_BRANCH: automation/refresh-snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data docs reports
          if git diff --cached --quiet; then
            echo "No snapshot changes to publish."
          else
            git commit -m "data: refresh npm CLI trends snapshot"
            git push --set-upstream origin "$REVIEW_BRANCH"
          fi

      - name: Add maintainer review link
        env:
          GH_SERVER_URL: ${{ github.server_url }}
          GH_REPOSITORY: ${{ github.repository }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          {
            echo "## Snapshot refresh"
            echo
            echo "Review or open the persistent refresh pull request:"
            echo "$GH_SERVER_URL/$GH_REPOSITORY/compare/$DEFAULT_BRANCH...automation/refresh-snapshot?expand=1"
          } >> "$GITHUB_STEP_SUMMARY"
